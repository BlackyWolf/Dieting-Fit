/**
 * The default page used to generalize a users daily/weekly/monthly data and
 * also the default page after login.
 */
export const Dashboard = () => {
    return (
        <h1>Dashboard</h1>
    );
};
